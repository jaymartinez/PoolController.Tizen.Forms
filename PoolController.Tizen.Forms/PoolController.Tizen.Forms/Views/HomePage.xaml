<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PoolController.Tizen.Forms.Views.HomePage">
    <ContentPage.Content>
        <StackLayout Orientation="Vertical">
            <ListView x:Name="HomeItemsListview" ItemSelected="OnItemSelected" HorizontalOptions="StartAndExpand">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout Padding="8" HorizontalOptions="Fill">
                                <Button Text="{Binding Name}" HeightRequest="50" />
                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>